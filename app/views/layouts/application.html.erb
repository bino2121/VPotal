<!DOCTYPE html>
<html>
  <head>
    <title>VPotal</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <%if session[:user_id]%>
      <%="ログインユーザー:#{@login_user.name}"%>
    <%end%>
    <div class = "header">
    <h1><%=link_to("VPotal","/")%></h1>
    </div>
    <%= yield %>
  </body>
</html>
